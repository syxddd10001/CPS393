#include <stdio.h>
#include <stdlib.h>
#include <string.h>
char* newString(char* str1, char* str2, char* str3, char* str4)
{
    int strLength = XXXXXXXX+1; // remember the + 1 for \0 
    char* newStr = XXXXXXX
    XXXXXXX
    XXXXXXX
    XXXXXXX
    XXXXXXX
    return newStr;

}

int main (void) {
    char *S = newString("ABcd","efgHI");
    printf("Called newString with ABcd and efgHI\n");
    printf("It returned: %s\n", S);
    printf("with length: %ld\n", strlen(S));
    return 0;
}
